[package]
name = "millet-sim"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "millet_sim"
path = "src/lib.rs"

[[bin]]
name = "msim"
path = "src/main.rs"

[dependencies]
millet-core = { path = "../millet-core" }
# `msim` takes source too, and assembles it; the library half stays free of it.
millet-asm = { path = "../millet-asm" }
